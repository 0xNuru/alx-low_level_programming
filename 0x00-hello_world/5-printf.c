#include <stdio.h>
/**
 * main - Entry point
 *
 * use the printf fxn to print a line
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
			return (0);
}

